# External Services Checklist

- Set timeouts on external calls — prevents cascading failures.
- Implement retries with backoff — improves resilience.
- Handle partial failures — avoids total outages.
- Log third-party errors separately — improves diagnosis.
