# Performance Checklist

- Add caching where appropriate — reduces latency.
- Paginate large responses — avoids memory pressure.
- Offload heavy tasks to background jobs — improves response time.
- Set request timeouts — prevents resource exhaustion.
- Use connection pooling — improves throughput.
