# Testing Checklist

- Write unit tests for core logic — ensures correctness.
- Add integration tests for APIs — validates flows.
- Test authorization boundaries — prevents access bugs.
- Test failure scenarios — improves resilience.
- Automate tests in CI — prevents regressions.
