# Deployment Checklist

- Use environment-based configs — avoids hardcoding.
- Validate required env vars on startup — prevents crashes.
- Run with least privilege — reduces blast radius.
- Add health check endpoints — enables monitoring.
- Ensure zero-downtime deploys — avoids outages.
