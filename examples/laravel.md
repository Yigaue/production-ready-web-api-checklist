# Laravel Security Examples

- Use `App\Http\Middleware\TrustProxies` to configure proxy headers.
- Use `Illuminate\Validation` for request validation.
- Use Laravel policies for authorization enforcement.
- Use `rateLimiter()` for API throttling.
